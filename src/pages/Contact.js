import React from 'react';
import ConsultationTextBox from '../components/ConsultationTextBox';

const Contact = () => (
    <div>
    <ConsultationTextBox/>
    <button className="button button__accent" to="/app">Try now for free</button>
    </div>
)

export default Contact;
